# Open-Question Log — Proposed 2026 PPNR Net-Interest Methodology

**Deliverable 9 of Phase 1 (seeded in Task 1).** Date: 2026-07-16.
Rules: every entry cites the source `(PDF p. N; md sec-M)`, states what is unknown or ambiguous, and what would resolve it. Entries are never deleted — resolved items move to status RESOLVED with the resolution recorded. Unknowns stay UNKNOWN in the handbook until resolved here.

## Decision log (user decisions binding on all deliverables)

| ID | Date | Decision | Where applied |
|---|---|---|---|
| D-001 | 2026-07-16 | The five interest-income components "Deposits with banks and other", "U.S. Treasuries", "Mortgage-backed securities", "Other securities", and "Other interest/dividend-bearing assets" are **five separate sibling models**, following Table A6 (PDF pp. 168–169) — the nested list in PROJECT_CONTEXT.md is a formatting slip, not a grouping directive | model-inventory records #2–#6; source map §4 |
| D-002 | 2026-07-16 | Firm fixed effects for the two proposed regression models (Table A9 discloses macro coefficients only): **backsolving α_b from launch-point actuals is the handbook's working method**, always flagged INTERPRETATION / CODING CONSIDERATION and never attributed to the Fed. The Fed's non-disclosure remains the SOURCE-STATED FACT. **Revised 2026-07-20:** superseded for `ie_other_borrowing` by PID-OB-5 (nine-quarter cumulative calibration); remains the working method for `nii_trading_al` (#11) only | inventory #11, #12; OQ-009; OQ-023; chapter template §3.3 |
| D-003 | 2026-07-16 | Chapter granularity: **one chapter per Table A6 net-interest component (~13 chapters)**; the loans chapter carries the six portfolio sections inside one common framework; shared securities/deposit mechanics handled by cross-references and the common-methodology chapter | chapter template header; future chapter plan |
| D-004 | 2026-07-17 | **Annualized-rate convention (project-wide):** all interest rates in the project are annualized; a quarterly dollar flow divides the annualized rate by four at the final conversion step only (simple nominal quarterization, not effective compounding); model equations/recursions operate in annualized units. Never presented as a Fed statement; where the source states its own conversion, the source governs | OQ-006 resolution; ie_dom_time_dep brief §7.9 (PID-6); all five liability-side IE chapters/specs (integration 2026-07-17); all future net-interest chapters |
| D-005 | 2026-07-17 | **Terminology: "launch point" (symbol PQ0) replaces "lift-off"/"q0" as the handbook's own term** for the last quarter before the projection horizon; coding identifiers use `launchpoint` (amending the PID-2-era canonical name `domestic_time_deposit_rate_liftoff` → `domestic_time_deposit_rate_launchpoint`). Verbatim Federal Reserve quotes and equation transcriptions keep the source's own words and notation ("at lift-off", "jump-off quarter", "$q0$") — source terminology is never rewritten | All chapters, briefs, specs, reviews; chapter-template; inventory prose; applied 2026-07-17 |
| D-006 | 2026-07-21 | **Canonical money unit (project-wide): USD millions at the model boundary; tidy-sheet money rows declare their unit.** Physical sources disagree — FR Y-14Q Schedule G balances are reported in millions while the FRB-provided projection paths (`frb_*`, OQ-023) arrive in billions — so every balance/dollar tidy-sheet row declares `scale` = `millions` \| `billions` and ingestion normalizes (×1000 for billions); blank or unconfirmed money units are refused, mirroring the percent/decimal rule for rates; months rows keep `scale` blank. Companion tripwire: the Other Borrowing implied-path step hard-fails when cumulative FRBTotal and cumulative sibling expenses differ by ≥50× (probable unit mismatch that the closed-form α_b would otherwise silently absorb, since the PID-OB-5 backsolve makes the final reconciliation exact by construction; the ≥50× screen and the pre-existing \|α_b\| ≥ 0.5 warning are project safeguards, never Fed statements) | `normalize.apply_money_scale`; tidy-sheet loader; `ie_other_borrowing.implied_other_borrowing_path` (`UNITS_MISMATCH_RATIO_GUARD`); company template; synthetic fixtures; schemas docstring |
| D-007 | 2026-07-21 | **Two-sheet firm-input contract (replaces the single tidy sheet).** Firm inputs split by time dimension: a **spot sheet** (`model, field, subcomponent, scale, value`) for one-time launch-point scalars, and a **wide quarterly sheet** (`model, field, subcomponent, scale, PQ1..PQ9`, one row per series, scale declared once per row, a supplied row fills all nine quarters) for projection-quarter paths — currently the three FRB `family` paths (OQ-023). Sheet placement is validated both ways (a quarterly field in the spot sheet, or a spot field in the quarterly sheet, is a hard error); config wires them as `[firm_data.spot]` + `[firm_data.quarterly]` (two CSV files or two tabs of one workbook). D-006 scale rules unchanged; a PQ0 column can be added later without breaking the layout. Never presented as a Fed statement | Ingestion config + tidy-sheet loader; company template; synthetic fixtures (`firm_inputs_spot.csv`, `firm_inputs_quarterly.csv`); architecture doc |
| D-009 | 2026-07-23 | **Chapter format: the compact 13-section skeleton is the canonical chapter structure from the asset side onward** — 1 Status and purpose · 2 Model summary · 3 Inputs (3.1/3.2/3.3) · 4 Timing and dimensions · 5 Equations and variable definitions · 6 Calculation workflow · 7 Output calculation · 8 Fed-stated assumptions and limitations · 9 User-confirmed implementation mappings · 10 Validation requirements · 11 Dependencies and hedge interface · 12 Open issues · 13 Key source references; regression-model chapters add the two `ie_other_borrowing` sections (Estimation versus projection; Firm fixed-effect treatment). The 14-section template form is retained as the recorded historical structure of the pilot chapter (`ie_dom_time_dep`, not rewritten). Banner block, labels, and citation conventions unchanged | `handbook/chapter-template.md`; first applied: `ii_dep_banks_other`, `ii_other_ida` chapters (2026-07-23); all future asset-side chapters; `inventory/asset-side-model-matrix.md` |
| D-008 | 2026-07-22 | **FRB expense-path sign convention.** User-stated: the FRB-provided total-interest-expense path is entered as **negative** amounts (income positive; NII = income + expense under that entry convention). The canonical convention stays **positive-magnitude expense**, matching the five models' outputs; the entry sign is declared in config — `[firm_data].frb_expense_sign` = `positive` (default) \| `negative` — and ingestion negates the expense path when `negative`, while income and NII pass through as-entered (the NII identity guard is convention-consistent after normalization). A canonical expense path negative in **every** projection quarter is refused at the schema boundary with a pointer to the setting — an undeclared sign flip would otherwise be silently absorbed into the PID-OB-5 α_b, the same failure class D-006 guards for units. Never presented as a Fed statement | Ingestion config + firm-data loader; `schemas.FamilyInputs` guard; company template; OQ-023 file-mapping context |

| D-010 | 2026-07-30 | **Loans workstream conventions (slice 1).** (a) **Scope:** the loans work proceeds as common framework → Wholesale (Corporate∩CRE shared) → Corporate → CRE → Wholesale integration review, each gated on user review; Retail portfolios (Mortgage, Auto, Credit Card, Other Consumer Products) are out of scope for slice 1 — retail rules are identified, cited, and deferred, never elaborated. (b) **Equation ownership:** the common brief transcribes **all** of Equations A32–A38 verbatim exactly once, following the source's own placement; sibling briefs (wholesale, and later corporate/CRE) cite them and transcribe none — Equation A37 stays in the common brief despite its wholesale-only title. (c) **Filing policy:** open questions and source quirks discovered while drafting are appended to `handbook/open-questions.md` and `inventory/source-integrity-review.md` **with final IDs at drafting time** (both logs are append-only), so briefs cite settled IDs rather than local candidates. (d) **Brief skeleton:** the `ie_dom_time_dep` 0–14 structure plus completion checklist (D-009 governs *chapters*, not briefs); the loans common brief adds a **Common-boundary register** classifying every rule in PDF pp. 173–188 as COMMON / WHOLESALE / CORPORATE / CRE / RETAIL-deferred, which is the placement authority for the whole sibling set. Chapter granularity is unchanged — one loans chapter with six portfolio sections (D-003) | `handbook/models/ii_loans_common.source-brief.md`; `handbook/models/ii_loans_wholesale.source-brief.md`; future corporate/CRE briefs and the Wholesale integration review |

### Project implementation decision (PID) registry — component-scoped, user-confirmed

PIDs are user-confirmed project implementation decisions, **never attributable to the Federal Reserve**. They were confirmed in the component sessions on the dates shown and registered here at the liability-side integration gate (2026-07-17). Project-wide decisions remain D-numbered above; full decision text lives in the cited chapter/spec.

| ID | Date | Model | Decision (one line) |
|---|---|---|---|
| PID-1 | 2026-07-17 | `ie_dom_time_dep` | Balance = FR Y-14Q Schedule G item **34E**, launch-point value held flat ("34E" appears nowhere in the Fed source) — chapter §1/§3.1 |
| PID-2 | 2026-07-17 | `ie_dom_time_dep` | Superseded by **D-005** (lift-off → launch-point naming; row kept for numbering continuity) |
| PID-3 | 2026-07-17 | `ie_dom_time_dep` | Schedule G item 71 WAL is reported in **months** — OQ-008 resolution |
| PID-4 | 2026-07-17 | `ie_dom_time_dep` | WAL_quarters = WAL_months / 3; ρ_b = 3 / WAL_months — OQ-008 resolution |
| PID-5 | 2026-07-17 | cross-model pattern | Scenario source = MEV workbook (Date column + one column per MEV); column "USD 1Y Treasury" confirmed for `ie_dom_time_dep`; the 3M-Treasury and BBB column names remain UNCONFIRMED working assumptions |
| PID-6 | 2026-07-17 | `ie_dom_time_dep` | D-004 applied to this component: QuarterlyExpense = AverageBalance × Rate / 4 at the final step only — OQ-006 resolution |
| PID-ODD-1 | 2026-07-17 | `ie_other_dom_dep` | Subcomponent balances = Schedule G items **34B/34C/34D** (source names no items) — OQ-016 |
| PID-ODD-2 | 2026-07-17 | `ie_other_dom_dep` | Total average balance = MDRM sum **BHCB3187 + BHOD3187 + BHCB2389 + BHOD2389** (source names no item; different physical source from the A47 weights — consistency monitor, not identity) — OQ-016. **Multiplicand role superseded by PID-ODD-3 (2026-07-23);** the mapping is retained as the optional consistency-monitor reference |
| PID-ODD-3 | 2026-07-23 | `ie_other_dom_dep` | **Expense multiplicand = Σ(items 34B + 34C + 34D)** — the same balances that weight Eq A47, so expense ≡ Σᵢ rateᵢ × balanceᵢ. Confirmed empirically against the company reference implementation (all per-subcomponent and aggregate rates verified identical; the reference matched the weight-sum expense, not the PID-ODD-2 MDRM-sum expense). The PID-ODD-2 MDRM sum becomes an optional monitor-only reference — OQ-016 |
| PID-FFR-1 | 2026-07-17 | `ie_fed_funds_repo` | Balance = items **36A + 36B**; the source-stated 44A/44B are Schedule G liability *rate* items (SQ-16), never used as balances — chapter §2.1 |
| PID-OB-1 | 2026-07-17 | `ie_other_borrowing` | **Superseded by PID-OB-5 (2026-07-20; row kept for numbering continuity).** Was: α_b backsolve identity at PQ0: α_b = R_actual(b,0) − Treasury3m(0) − β1·BBB(0) − β2·CP(b,0) − β3·Subdebt(b,0) — OQ-009 |
| PID-OB-2 | 2026-07-17 | `ie_other_borrowing` | Physical balance = Schedule G items **36C + 38 + 39** (Fed-stated 44C/46/47 preserved as the [FACT] scope; tension recorded, not resolved) |
| PID-OB-3 | 2026-07-17 | `ie_other_borrowing` | **Superseded by PID-OB-5 (2026-07-20; row kept for numbering continuity).** Was: backsolve actuals = single PQ0 quarter; R_actual(b,0) = 4 × QuarterlyExpense_actual(b,0) / B(b,0) under D-004 — OQ-009 |
| PID-OB-4 | 2026-07-17 | `ie_other_borrowing` | BBB physical source = project MEV file; four details TO BE CONFIRMED before first use (column name; yield vs. spread; any Treasury-yield addition; unit scale) |
| PID-SEC-1 | 2026-07-23 | securities family (#3–#5) | **Input granularity = security-level.** User-stated: the firm holds full security-level data for every position. Inputs arrive per security via a positions contract (third-sheet layout, finalized on receipt of the user's format file — pending upload to `config/local/`); supersedes the pre-aggregated-bucket proposal. Aggregation to component level happens inside the model, mirroring the Fed's per-security calculation |
| PID-MBS-1 | 2026-07-23 (refined 2026-07-24) | `ii_mbs` | **Agency RMBS vendor input = per-security projected current-face path in USD** — the delivered prepayment sheet is a pivot ("Sum of current_face"; Row Labels = CUSIP matching the positions sheet's IDENTIFIER_VAL; columns = month offsets 0,3,…,27 ≙ PQ0..PQ9; a Grand Total column is ignored), monotone non-increasing dollar faces. Equivalent to the originally described survival factor via factor(i,q) = Face(i,q)/Face(i,0); the dollar-face form is adopted as canonical. Income is computed by the Equation A41 terms on this face path (coupon accrual per PID-SEC-4). Validation: prepayment Face(i,PQ0) reconciles against the positions sheet's CURRENT_FACE_VALUE. Load rule (user-directed 2026-07-24): a prepayment row with **PQ1 face = 0 is skipped**, logged per row; the security, if still in scope, then follows the flat-face no-prepayment treatment. Resolves OQ-026 for project implementation |
| PID-SEC-2 | 2026-07-23 (finalized 2026-07-24) | `ii_mbs`, `ii_other_sec` (floaters) | **Floater negative-margin floor — three config-switchable modes** (user-specified; company convention, never a Fed statement — the Fed imputation has no floor). Scope: floaters whose launch-point imputed margin is negative (t=0 coupon < t=0 spot 3M Treasury). Config `floor_mode` ∈ { `zero` — floor projected coupon at 0; `security_floor` — floor at the security's coupon floor when available (no flooring if absent); `none` — no floor, negative projections allowed }. One mode active per run, declared in the company-local config. **Mode 4 added 2026-07-24 — `security_floor_else_zero`, the reference-workbook rule inferred from the user's compare observations:** EVERY floater (not only negative-margin) is floored at max(margin + 3M(q), security coupon floor if on file else 0). Evidence: negative-margin Agency VARIABLE rows accrue PQ1 then nothing (no floor on file → 0); Non-Agency RMBS floaters show "PQ1 bigger, PQ2–9 constant smaller" (floor binds from PQ2); CMBS floaters show "constant PQ1–9" (floor binds from PQ1); the CLO/Student-Loan/Auto/Foreign-RMBS few-percent-low cluster matches positive-margin floaters whose floors bind as 3M falls. **Compare outcome 2026-07-27:** floors matter where they exist (CLO moved with each floor-source change; the PID-SEC-9 main-sheet floor column governs where populated), but the mode is **REFUTED as the Non-Agency RMBS / CMBS explanation** — those totals were immobile across all three floor sources (no floor on file anywhere), so the reference projects their floating coupons by some other rule; under investigation via the compare mode's FLOAT-RULES bake-off. **Bake-off round 1 (2026-07-27):** the reference's floor column is ≈ 0 everywhere it matters (FLOOR-SUSPECT 0; CLO reverts to the no-floor ratio exactly) — **the reference does not floor floaters at security floors at all**. Non-Agency RMBS floaters ≈ **launch-coupon-flat** (flat_c0 = 1.0114); Municipal floaters ≈ **book-yield-flat** (1.0000, n=5); CMBS and Agency VARIABLE sit **between** the spot-margin path and the PQ1-anchored path — the signature of a **prior-quarter reset** (coupon(q) = margin + 3M(q−1): PQ1 accrues at the launch coupon fixed at the last reset). Round 2 adds rules lag3m_f0 and excel_ind (split by the sheet's own float/fixed indicator, PID-SEC-9 4th column) to test whether one physical rule + the reference's own classification explains all categories. **Round-2 outcome (2026-07-27): the indicator marks EVERY floater Float (ind 0/898/0 etc.) — the classification-split hypothesis is refuted; lag3m_f0 improves but stays low (Agency 0.57, CMBS 0.70, RMBS 0.17) while lag1_3m overshoots — the reference sits between; RMBS alone matches flat_c0 (1.0114); the Municipal per-row spread bottoms at exactly 1/1.266 (p10 = 0.794 — the 21 % tax-equivalent gross-up factor) but is smooth, not single-cluster. Round 3 instruments identification directly: IMPLIED-CPN (the reference's implied coupon path = (II_PQ − our AA) ÷ face-weight, as a ratio to the launch coupon, per quarter per category/rate-type) + the public SCENARIO-3M path + candidate freeze1_f0 (reset once at PQ1 then frozen — the only rule that is per-row-constant while sitting between the lag rules). **Round-3 identification (2026-07-27; 3M path 4.40 % → 1.80 % → 0.10 % flat):** every floater category's implied coupon is EXACTLY FLAT from PQ2 (Auto 0.252, CLO 0.262, CMBS 0.437, Foreign 0.227, SLABS 0.264 of the launch coupon) — **PQ2–9 = the spot margin + 3M(q) rule, margin vs 3M(PQ0), as we implement it**; the entire residual sits in PQ1, whose implied index ≈ 2.4–2.7 % (between 4.40 and 1.80) — the **monthly-reset blend** signature (PQ1 ≈ ⅓ launch coupon + ⅔ new rate; candidate `blend13`); Non-Agency RMBS floaters' implied ≈ 0.99 flat at the launch coupon → **negative-margin floaters are never projected** (candidate `neg_hold`); Municipal fixed implied = 1.131 CONSTANT and Municipal floating = 1.189 constant (= book yield) → a level multiplier on the muni coupon input, suspected **tax-equivalent coupon column** (CPN-SOURCE diagnostic added: sheet coupon ÷ ITO coupon per category). Round 4 tests blend13/neg_hold + CPN-SOURCE |
| PID-SEC-3 | 2026-07-23 | securities family | **Unsettled-transaction AC proxy** (company convention): securities with settle date very close to the reporting date (trigger: maturity, book yield, or amortized cost missing/zero) use **purchase price/100 × notional** as the amortized-cost proxy. Note (2026-07-24): CQSCP093 is the **Pricing Date** (mm/dd/yyyy), not a price. Price = the `Price` column (CQSCJH21), **per-100 confirmed** (user: "always around 100 ish"); **notional = CURRENT_FACE_VALUE (user-confirmed 2026-07-24)**. Proxy securities carry accretion = 0 (just-purchased near par), flagged in warnings |
| PID-SEC-4 | 2026-07-23 | securities family | **Coupon-accrual face convention**: CouponAccrual(q) = CouponRate(q) × **prior-quarter end-of-period current face value** — the user-confirmed operationalization of the Fed's "beginning-of-the-period current face value at time t" (PDF p. 192); pins the indexing so paydowns affect accrual from the following quarter |
| PID-SEC-5 | 2026-07-24 | securities family | **Category→model mapping and policy** (user-confirmed): FR Y-14Q Schedule B.1 `SECURITY_DESCRIPTION_1` (CQSCP084) drives assignment — *US Treasuries & Agencies* → `ii_ust`; *Agency MBS* → `ii_mbs` prepayment category; *CMBS / Domestic Non-Agency RMBS (incl HEL ABS) / Foreign RMBS* → `ii_mbs` non-prepayment; equity-intent types (*Mutual Fund, Common Stock*; accounting intent EQ) → **out of scope**; all other debt categories → `ii_other_sec`. **Unmapped/unknown categories are a hard error — surface and ask, never default.** Agency MBS positions **absent from the prepayment sheet carry no prepayment** (face held flat — user-stated: those are multi-family). Full table: `specifications/interest-income/securities/securities-input-contract.md` |
| PID-SEC-6 | 2026-07-24 | securities family | **Native-workbook input contract** (user-stated: the sample format "matches what we will have eventually"): one workbook read as-is — (1) a Schedule B.1-layout **positions sheet** (3-row header: technical name / description / MDRM; data from row 5; 22 columns incl. ACCOUNTING_INTENT CQSCP092, PRICING_DATE CQSCP093 mm/dd/yyyy, BOOK_YIELD CQSCP094, PURCHASE_DATE CQSCP095, CURRENCY CQSCS371, Price CQSCJH21, TRANSTYPE); (2) one or more **enrichment tabs** (region-split allowed) keyed by CUSIP/ISIN: maturity date (Excel serial), coupon rate (percent; user-stated never blank), rate type FIXED/FLOATING/ZERO COUPON, coupon floor (may be blank), WAL (years, decimal), floater flag, currency; (3) the **prepayment pivot sheet** (PID-MBS-1). **Balances are already USD-converted** (user-stated — FX implicitly frozen; no FX machinery in-model). Sheet names/paths are company-local config; the committed contract documents layout only. **Amendment 2026-07-27 (user-directed): Excel formula-error literals (#VALUE!, #N/A, #REF!, #DIV/0!, #NAME?, #NULL!, #NUM!) are treated as MISSING values** — the sheet's derived columns error when their own inputs are absent (those rows lack the coupon etc. as well), so the affected field falls back per its normal chain; a per-run census warning counts them per field, never silent. Contract: `specifications/interest-income/securities/securities-input-contract.md` |
| PID-SEC-7 | 2026-07-24 | `ii_mbs` (Agency MBS) | **WAL-as-maturity fallback** (user-confirmed; company convention): an Agency MBS whose enrichment row has **no maturity date** uses its **WAL as the maturity in years** — maturity_quarters = ceil(4 × WAL), min 1, logged per security. Maturity dates remain the primary source everywhere; the fallback is scoped to Agency MBS only. This removes the maturity-missing leg of the PID-SEC-3 trigger for those securities (the earlier "no price to proxy from" stop) |
| PID-SEC-8 | 2026-07-24 | securities family | **Reference-verified calculation scheme** (user verified against the company workbook): income is ALWAYS coupon accrual + accretion/amortization (AA) + hedge income + reinvestment income; **AA = 0 if the quarter's face is 0, else (prior-quarter face − prior-quarter AC) ÷ denominator** with denominator = 4 × maturity years (day difference ÷ 365; WAL(t=0) for Agency MBS; PQ0 numerator AND denominator for U.S. Treasuries — a constant amount, matching Eq A40); **New AC = prior AC + AA − principal paydown**; the collapsed A42 book-yield form is NOT used (the Fed-stated effective-interest method for other MBS/other securities is preserved as [FACT] in the chapters — the reference implements the split form, a recorded divergence); prepayment applied **per row** as a survival factor on each lot's own launch face; prepayment blank cells are 0 with no warning. Supersedes the interim effective-interest/BY-shift/survival-scaling choices. **Compare-mode confirmations (2026-07-24):** the U.S. Treasuries denominator is **whole maturity quarters** (empirical 0.05 % match; the fractional day/365 divisor overstated short-maturity accretion); paydown reinvestment validated (Agency MBS ours/ref = 1.0055); PID-SEC-2 mode `zero` semantics match the reference (negative-margin floaters accrue PQ1 while the 3M exceeds \|margin\|, then floor at 0); STEP CPN confirmed flat at the launch coupon; multifamily flat-face confirmed (the earlier "PQ2–9 = ¼ of PQ1" observation was withdrawn by the user 2026-07-24 — accrual is constant PQ1–9). **Reference-income scope resolved (2026-07-24, dual ratios):** the workbook's II_PQ1..9 columns **EXCLUDE** reinvestment income (booked only in the separate "Reinvestment Interest income" section) — UST ours-excluding-reinvestment/ref = 0.9985 vs 1.0081 including; Agency MBS 0.9975 vs 1.0055; compare mode's primary ratio is therefore xr/ref |
| PID-SEC-9 | 2026-07-24 | securities family | **Positions-sheet technical input columns** (user-directed; company convention): the reference workbook's positions sheet carries its own model-input columns on the MDRM header row — a maturity-in-years column, a coupon column (decimal), and a coupon-floor column (decimal). Config-declared by exact header text (`positions_maturity_years_column` / `positions_coupon_column` / `positions_floor_column`); DECIMAL units, no scale applied. Precedence: the enrichment **maturity date stays primary** and the years column is the **fallback** when the date is missing (feeds the PID-SEC-8 accretion denominator AND event timing, quarters = ceil(4 × years) — removes the AA-held-at-0 gap on ~1,000 rows incl. Municipals; scoped correctly, this is the user's earlier "use maturity(yr)" request); the enrichment **coupon stays primary** and the coupon column **fills blanks** (rescues the 17 FLOATER-NO-COUPON ARS/CLO rows); the **floor column is PREFERRED** over the enrichment floor when non-blank (it is the reference's own floor input — drives `security_floor_else_zero`). Declared-but-absent column = hard error; PID-SEC-7 (Agency WAL) applies only after both date and years column are missing. **4th column (2026-07-27): `positions_rate_type_column`** — the sheet's own float/fixed indicator, carried **verification-only** (`excel_rate_label`: compare bake-off + ITO-disagreement monitor); the enrichment rate type keeps driving the models until a PID adopts the indicator |
| PID-SEC-10 | 2026-07-27 | `ii_mbs`, `ii_other_sec` (floaters) | **Floating-coupon projection modes** (config `floating_projection`; reference-identified, pending confirmation rerun): `spot` — the original margin + 3M(q) every quarter; `neg_hold` — a **negative-launch-margin floater is never re-projected** (launch coupon held flat PQ1–9; reference-observed via IMPLIED-CPN ≈ 0.99 flat for Non-Agency RMBS floaters; consistent with the user's "coupon floor applied" statement when the effective floor is the security's own current coupon — legacy HEL floaters sit at their lifetime floors); `neg_hold_blend13` — additionally PQ1 = ⅓·launch coupon + ⅔·(margin + 3M(PQ1)) for positive margins (monthly-reset blend: the first month of PQ1 accrues at the coupon set before launch; reference-identified via the implied PQ1 index ≈ 2.4–2.7 % between 3M(PQ0)=4.40 % and 3M(PQ1)=1.80 %), spot thereafter. Floor modes still apply on projected paths. Never a Fed statement — the Fed imputation is the plain spot rule. **Amended 2026-07-27 (round-5 run): the reference treats CATEGORIES differently — two more modes (`blend13` = PQ1 blend without the hold; `flat_c0` = every floater held at the launch coupon) and a per-category `floating_projection_overrides` config map. Round-5 evidence:** blend13 alone lands Auto 1.0092 / CLO 1.0139 / Foreign RMBS 1.0121 (the neg-hold leg WRECKED CLO — held low-coupon floaters exploded to 1,300–1,700 % on 16 rows); Non-Agency RMBS wants flat_c0 (1.0114 — even positive margins held; neg_hold alone leaves it 7 % low); CMBS best fit = neg_hold_blend13 (0.83, bands sharply improved); Agency floaters: no candidate fits (freeze1 0.894 closest) — category default `spot` kept (best category total 0.9975). Recommended overrides: RMBS→flat_c0, CLO/Auto/Foreign-RMBS/Student-Loan→blend13, CMBS→neg_hold_blend13. **Sixth mode added 2026-07-28 — `freeze1`:** PQ1 accrues at the launch coupon (the rate fixed at the last reset before the launch date), then the coupon resets ONCE off 3M(PQ1) and is **frozen** there for PQ2–9, floored at 0 (the floor is part of the priced rule). Identified for **Agency MBS floaters**, the category that had defeated every earlier candidate: in the 2026-07-28 full run the bake-off prices `freeze1_f0 = 0.9907` against the configured `spot` rule's **0.4627** over 898 rows — a −101.8 USD mm gap that freeze1 takes to about −1.8. (The earlier round-5 reading of 0.894 for freeze1 predated the PID-SEC-8/PID-SEC-9/PID-SEC-13 fixes.) Recommended addition: `"Agency MBS" = "freeze1"` (confirmed 2026-07-28: Agency MBS moved 0.9914 → **0.9989**). **Seventh and eighth modes added 2026-07-28 — `pos_hold` / `pos_hold_blend13`, the MIRROR of `neg_hold`:** a **POSITIVE**-margin floater is the one never re-projected, while a negative-margin one follows the projected (and floored) path. Reference-verified for **CMBS** on 8 rows from the gaps CSV: `coupon × face/4 + our AA` reproduces the workbook's constant quarterly amount to within **0.08 %**, and a negative-margin CMBS row shows the reference accruing a partial PQ1 then **nothing** — projected and floored, not held. This explains why `neg_hold_blend13` sat at 0.8271 while `flat_c0` overshot at 2.1208: the reference holds exactly the opposite subset. Economic reading: with 3M falling from the launch spot all quarter, `margin + 3M(q)` never exceeds the launch coupon, so a positive-margin floater behaves as if its coupon cannot reset downward. Recommended addition: `"CMBS" = "pos_hold_blend13"`. **REFUTED AND REVERTED 2026-07-28 on the confirmation run:** CMBS went **0.9148 → 1.4135** (ours 781.51 → 1207.50 against a reference of 854.21) and the per-row spread went from p50 = 1.000 / p90 = 1.014 to p75 = 3.240 / p90 = 3.981. The eight rows the rule was verified on came from the **top-25-by-gap** section of the gaps CSV — a deliberately biased sample of the rows that were already wrong — while the ~180 CMBS floaters sitting at exactly 1.000 under `neg_hold_blend13` were correct and got broken. The verified observation stands (those ~15 rows really are held flat by the reference); what is refuted is that margin sign is the discriminator. **Keep `"CMBS" = "neg_hold_blend13"`**; the ~15-row subset is still open and needs a discriminator found on the WHOLE population, not the tail |
| PID-SEC-11 | 2026-07-27 | `ii_other_sec` (Municipal Bond) | **Book-yield accrual categories** (config `book_yield_categories`; reference-identified, pending the user's formula check): configured categories accrue at the Schedule B.1 **book yield held flat** instead of the coupon — fixed AND floating alike; zero-coupon rows untouched; missing book yield falls back to the coupon with a surfaced warning. Evidence: Municipal floaters matched book-yield-flat EXACTLY (bake-off by_flat = 1.0000, n = 5); Municipal fixed implied a **time-constant** multiplier 1.131 (= BY/coupon on face); the ITO coupon equals the main-sheet coupon (user-verified 2026-07-27), eliminating the input-difference (tax-gross-up) hypothesis. **REFUTED AS THE MUNICIPAL MECHANISM 2026-07-27:** the user confirmed the muni accrual formula references the ITO coupon, NOT book yield, and the BY-accrual run split the population (per-row xr/ref p25 = 0.991, p50 = 1.009 — half now correct — but p75 = 1.436, p90 = 1.746 badly high; category 1.1383 vs 0.8870 before). Together with the coupon-accrual run's p10 = 0.794 = exactly 1/1.266, the surviving hypothesis is a **tax-equivalent gross-up (× 1/(1 − 21 %)) applied to the ITO coupon for TAX-EXEMPT munis only, plain coupon for taxable ones** — pending the user's check of the muni formula for a conditional ×1.266 / ÷0.79 factor and of any taxable-status flag column. The config feature is kept but the recommendation is now `book_yield_categories = []` |
| PID-SEC-12 | 2026-07-27 | `ii_other_sec` (Sovereign Bond ZCBs) | **Zero-coupon accretion suppression** (user-directed to match the reference): config `zcb_no_accretion_categories` — zero-coupon rows in listed categories book **no accretion income** (the user verified the reference's sovereign-ZCB GII cells are blank throughout, a few PQ1-only). The Fed-stated A42 accretion is preserved as [FACT] — this is a **recorded reference divergence**, per-run count logged. OQ-028 stays open for the workbook owners' rationale (deliberate exclusion vs booked elsewhere); recommended config: `["Sovereign Bond"]` |
| PID-SEC-13 | 2026-07-28 | securities family (blank amortized cost) | **Blank-amortized-cost treatment + PID-SEC-3 rescoping** (user-directed 2026-07-28, after the largest single-security gap in the Agency MBS compare was traced to it). Two configs: `missing_ac_mode` ∈ { `price_proxy_no_accretion` (the original PID-SEC-3 behavior — AC = price/100 × face, accretion held at 0; **DEFAULT**, so no number moves silently) \| `price_proxy_accrete` (same AC, accretion runs on the discount the price implies) \| `zero_accrete` (blank read as **ZERO**, whole face accretes over the PID-SEC-8 denominator — **what the reference workbook does**; recorded divergence) }, and `unsettled_window_days` (default 7). **Rescoping:** PID-SEC-3 defines itself as the *unsettled-transaction* proxy ("settle date very close to the reporting date") but its trigger (maturity **or** book yield **or** AC missing/zero) never tested a settle date, so every blank-AC row took the proxy and lost its accretion. `PURCHASE_DATE` (CQSCP095) is now loaded (it was in the PID-SEC-6 contract but never read) and rows within the window take the original PID-SEC-3 treatment **whatever the mode says**; rows outside it, and rows that cannot be classified (no CQSCP095 column, no report date), follow `missing_ac_mode` — with a per-run census warning. **Evidence (2026-07-28):** Agency MBS row 250.00 face / 2.5 % coupon / WAL 9.6556y / price 83.445, blank AC — reference books 63.5927 over 9Q, we booked 11.0191 (gap −52.5736, the single largest in the securities compare). Our own PID-SEC-8 recursion rerun with AC(t=0) = 0 reproduces the reference's AA leg to 0.0003/quarter and its 9Q total to −0.0001, confirming face path, coupon, 4 × WAL denominator, paydown-to-AC treatment and AA recursion all already agree — the *only* disagreeing input was the launch AC. Note the reference's behaviour books 52.57 of accretion on a 250 face (≈21 % cumulative on a 2.5 % coupon bond), so `zero_accrete` matches the workbook without being endorsed as economically right — hence a switch, default off, and OQ-031. **AMENDED 2026-07-28 after the first full run with the switch ON GLOBALLY — the mode is now PER-CATEGORY (`missing_ac_mode_overrides`, same shape as `floating_projection_overrides`):** the blank-amortized-cost population is only ~12 rows, and the reference does **not** treat them uniformly. One Agency MBS row accretes its whole face (63.5927 over 9Q, reproduced to −0.0001); ten **CLO** rows carrying the same blank have II_PQ ≡ **0** — the reference books no income for them at all. Running `zero_accrete` globally therefore closed −52.57 on the Agency row and opened **+216.72** across those ten CLO rows (CLO category xr/ref 1.0139 → 1.1140), a net worsening of ~164. Recommended config: global default `price_proxy_no_accretion`, override `{"Agency MBS" = "zero_accrete"}`. Note the ten CLO rows are the same `ref-zero` phenomenon as OQ-028 (sovereign ZCBs) and OQ-029 (ARS) — the surviving hypothesis is that the reference deliberately does not model those positions and the blank AC is a symptom, not the cause; folded into OQ-031 |
| PID-SEC-14 | 2026-07-28 | `ii_other_sec` (Municipal Bond) | **Collapsed Equation A42 categories** (config `a42_collapsed_categories`; reference-verified, resolves OQ-030). Configured categories are computed by **Equation A42 exactly as printed** (PDF p. 201): income(q) = **AmortizedCost × BookYield ÷ 4**, **constant PQ1–PQ9**, with **no separate accretion leg** — the amortized cost is not stepped. Applies to fixed, floating and zero-coupon rows alike; a missing book yield falls back to the PID-SEC-8 split form with a surfaced warning. **Evidence (2026-07-28, from the per-security gaps CSV):** across 20 reference rows spanning AC/face **0.774–1.193** — including premium rows where AC **exceeds** face — the reference's constant quarterly amount matches BY × **AC** ÷ 4 to within **0.10 %** (book yield is only carried to 4dp, so that residual is display rounding), while BY × **FACE** ÷ 4 is out by up to **29 %**. The reference's own II_PQ1..9 are identical across all nine quarters for every muni row, and the IMPLIED-CPN diagnostic showed a time-constant multiplier (1.131 fixed / 1.189 floating) — the signature of a constant amount against our declining split-form path. This is the **one category where the collapsed A42 form beats the PID-SEC-8 split form**; PID-SEC-8's "the collapsed A42 book-yield form is NOT used" stands for every other category (CMBS fixed `current` = 1.0000, Student Loan 1.0029). Recommended config: `["Municipal Bond"]`. Supersedes PID-SEC-11 for munis (that switch used FACE and added our AA leg, which is why it split the population); `book_yield_categories` stays empty. **CONFIRMED 2026-07-28: Municipal Bond ours 2967.13 vs reference 2967.11 — 1988 of 1992 rows inside 0.1 %, category xr/ref = 1.0000, and the securities total moved 0.9923 → 0.9996.** **Sovereign Bond is the same form [candidate 2026-07-28]:** BY × AC ÷ 4 reproduces its reference amounts to within 0.10 % on 6 of 7 sampled rows, but Sovereign also needs PID-SEC-15 — several of its rows are short-dated and the reference accrues fewer quarters than our `ceil`, which is why `by_ac_only` prices at 1.0642 rather than 1.0000 on the current quarter count. Enable the two together. **CONFIRMED 2026-07-28: Sovereign fixed rows reached xr/ref 1.0006 under A42 + floor.** One interaction bug found and fixed the same day: the A42 branch returned before the PID-SEC-12 check, so adding Sovereign Bond to `a42_collapsed_categories` revived its **zero-coupon** rows (bucket xr/ref **8.9590** against a reference that books exactly zero for 60 of them). PID-SEC-12 suppression now takes precedence over PID-SEC-14 for zero-coupon rows in a suppressed category |
| PID-SEC-15 | 2026-07-28 | securities family | **Maturity-quarter rounding** (config `maturity_quarters_rounding`; reference-observed, pending the confirmation rerun): how many quarters a security accrues — `ceil` (**default**, unchanged: `ceil(4 × maturity_years)`) | `floor` (only FULL quarters). **Evidence (2026-07-28, Sovereign Bond gaps CSV):** rows with `4 × maturity_years = 3.233` have reference income in exactly **3** quarters and zero thereafter, and a row at 2.773 has exactly **2** — while we book 4 and 3 respectively. Combined with PID-SEC-14's A42 form the short-dated Sovereign rows then reconcile: e.g. AC 496.13 × BY 0.0477 ÷ 4 = 5.9164 against the reference's 5.9157 per quarter, × 3 quarters = 17.75 against a reference 9Q total of 17.747. Kept a **global** switch defaulting to the old behaviour rather than applied silently, because it interacts with the Agency MBS truncation class (476 rows, xr/ref 0.8182, mean first dead quarter PQ7.3) where our projections already end **too early** — `floor` would end them earlier still. A/B both categories before adopting. **AMENDED 2026-07-28 — the switch is now PER CATEGORY (`maturity_quarters_rounding_overrides`).** Applying `floor` globally was a regression: it did fix Sovereign Bond (its fixed rows reached xr/ref **1.0006**, and FIXED-RULES `current` and `by_ac_only` both hit 1.0000) but cost **US Treasuries 544 USD mm** (0.9985 → 0.9766, 181 rows newly truncating at xr/ref 0.8707) and pushed Agency MBS 0.9989 → 0.9971. **Corrected 2026-07-28** — the first explanation offered (that shortening `maturity_quarters` also shrinks the Eq A40 accretion denominator) is **wrong for the rows that regressed**: for a security maturing inside the horizon the accretion total is invariant, since (face − AC)/M × M = face − AC. The whole 544 is **one quarter of COUPON on 181 rows** (181 × the ~3.10 USD mm average row-quarter = ≈561, against 544 observed). **Surviving hypothesis: the split is by income FORM, not by category.** The A42 collapsed form reads the amortized cost of the CURRENT quarter, which is zero in the maturing quarter → floor; the split coupon+AA form reads the PRIOR quarter's EOP face (PID-SEC-4), which is still positive in the maturing quarter → that quarter books → ceil. Consistent with everything seen: the categories floor helped are exactly the A42 ones (Sovereign; Municipal is A42 too but all its maturities exceed 9 quarters, so rounding never bit), and UST is split-form. If confirmed, rounding should key off `a42_collapsed_categories` rather than being configured separately — OQ-032. Only Sovereign Bond is evidenced — recommended config `{"Sovereign Bond" = "floor"}` with the global default left at `ceil` |
| PID-SEC-16 | 2026-07-28 | `ii_mbs` (Agency MBS) | **Vendor face path governs survival** (config `agency_face_path_survival`, default off). For an Agency MBS carrying a prepayment face path, the path — not the PID-SEC-7 WAL-as-maturity — decides how long the security lives, and a maturity event is booked only where the path itself reaches zero. **Rationale (user-supplied workbook methodology, 2026-07-28):** the reference sets a security's EOP current face to zero **in the quarter it matures**, and income is taken on the prior quarter's EOP balance, so the maturing quarter books nothing at all (no proration) — see PID-SEC-15. A **WAL is an average life, not a maturity date**: a pass-through with WAL 1.25y has a legal maturity decades out and the vendor path still carries balance at PQ9, so nothing should zero its face. Evidence: TRUNCATION-CLASS shows 476 Agency rows ending before PQ9 while the reference keeps accruing (xr/ref 0.8182, mean first dead quarter PQ7.3). PID-SEC-7's WAL keeps feeding the 4×WAL accretion denominator — only the survival role is removed. **FIXED 2026-07-29 — the switch now only EXTENDS.** As first written it took the face path's end unconditionally, so it could also SHORTEN a life, which is the opposite of its purpose: trailing **blank** cells in the prepayment pivot are read as 0 (PID-SEC-8) and are indistinguishable from a genuine paydown to zero. Measured effect with the switch ON: TRUNC Agency MBS went **476 rows @ mean first dead quarter PQ7.3 → 520 @ PQ5.5** — more rows dying earlier. It now extends past a WAL-derived maturity but never cuts before one, and the no-shorten case is logged per row. Income is unaffected in the shortened case either way, since coupon accrues on the PRIOR quarter's face, which is already zero. Still to be judged on a clean run: whether the switch earns its place at all |
| PID-SEC-17 | 2026-07-29 | `ii_mbs`, `ii_other_sec` (floaters) | **Supplied margin overrides the imputation** (config `positions_margin_column`, or `margin_column` on an enrichment block; user-confirmed). Where a security carries a real margin, `coupon(q) = margin + 3M(q)` uses THAT spread; the Fed's `margin = t0 coupon − t0 spot 3M` imputation remains the fallback for rows without one, and every substitution is logged with both values. **Source basis:** the imputation is explicitly a workaround for missing data — "the Board does not have reliable margin information for all floating rate instruments **(except for Agency residential mortgage-backed securities)**" (PPNR p. 199; `ii_mbs.md` §8 assumption 3). Agency RMBS are the stated exception, and the user confirmed 2026-07-29 that the workbook uses `Margin + 3M(q)` for them. This is why every imputation-based projection mode failed on those 898 rows: with an imputed margin of c0 − 4.40 % and 3M collapsing to 0.10 %, the projected coupon goes to roughly zero, which is how `spot` scored 0.4627. Supersedes the PID-SEC-10 bake-off for Agency MBS — those modes were fitting a projection rule to compensate for the wrong margin |
| PID-SEC-18 | 2026-07-29 | securities family (floaters) | **The floating rule is ONE rule, and the floor is the whole story** (user-supplied 2026-07-29): `coupon(PQx) = Max(CPN_Floor, Coupon Rate − PQ0 3M UST + PQx 3M UST)` for **all** floating securities — which is exactly the already-implemented `floating_projection = "spot"` with `floor_mode = "security_floor_else_zero"`. **Verified:** with `CPN_Floor = c0` the formula reproduces the worst Agency MBS row exactly (0.5181 vs reference 0.5181, and 0.5181 vs 0.5124 at PQ5 where the face has paid down) and the CMBS rows to within 0.08 %; with `CPN_Floor = 0` the coupon collapses and only the AA leg survives (0.0680 against 0.5124). **So the entire PID-SEC-10 mode family — spot / neg_hold / neg_hold_blend13 / blend13 / flat_c0 / freeze1 / pos_hold — was approximating a FLOOR that was not being read.** Root cause: PID-SEC-9 made the positions-sheet floor column *preferred when non-blank*, so a literal **0** there overrode a populated `CPN_Floor` on the enrichment tab. Fixed: a zero in the positions column is treated as not populated and falls through (a 0 floor and no floor are operationally identical under `security_floor_else_zero`), plus a **FLOOR-SOURCE** census reporting how many rows took a non-zero floor from each source and how many have a floor equal to their launch coupon. Recommended config: **empty `floating_projection_overrides`** |
| PID-LOAN-1 | 2026-08-03 | `ii_loans` (Corporate) | **The three data-limited Corporate portfolios are treated as floating (variable) rate.** Loans for purchasing and carrying securities, domestic farmland loans, and international farmland loans — the portfolios with no loan-level FR Y-14Q H.1 data and therefore no fixed/variable split (PDF p. 176; md sec-154) — are each projected as a single undivided block on the **variable-rate path** (Eq A33: three-month Treasury base rate + constant launch-point spread). "Floating" is the FR Y-14Q reported value for the document's "variable-rate" (footnote 62, PDF p. 175), so the decision is expressed in the source's own reporting vocabulary. **Fed-source status: partial concurrence** — the source states the variable-rate conclusion for *loans for purchasing and carrying securities* only ("The majority of NPMLs were variable-rate, so the Board assumes loans for purchasing and carrying securities have variable rates"); for the two farmland portfolios it states **no rate type at all**, and that absence is preserved as [FACT]. First PID for the loans component. Resolves OQ-036 for project implementation — chapter/brief §0.1, §6.4 |
| PID-LOAN-2 | 2026-08-03 | `ii_loans` (Corporate) | **Segment key and computation granularity.** The H.1 mapping's three columns form the reference key: **Fed Category (1–11) × Lower-of-Cost-or-Market flag {HFI, HFS} × Variable Type code {0 DO NOT USE, 1 Fixed, 2 Floating, 3 Mixed, 4 Entry Fee Based}**. The LOCOM flag is the physical realization of the Fed's HFI vs FVO/HFS asset classification (PDF p. 175). Rate pools and spreads are computed per **Fed Category × LOCOM**, separately for each rate type. The vocabulary contains **no demand-loan code**, so the Fed's "demand loans are treated as variable-rate" rule (PDF p. 176) has no counterpart in this data and is recorded as inapplicable rather than mapped. Independently corroborates the OQ-034 grid reconstruction — spec §3 |
| PID-LOAN-3 | 2026-08-03 | `ii_loans` (Corporate) | **Rate pools / initial interest rates.** Per Fed Category × LOCOM: `FloatPoolRate` = Σ(Committed Exposure Global × Interest Rate) ÷ Σ(Committed Exposure Global) over Variable Type ∈ {2 Floating, **3 Mixed**}; `FixedPoolRate` = the same over {1 Fixed} only. A row whose `Interest Rate` is NA or [NULL] **leaves both numerator and denominator**. The rate weighting field is **committed** exposure, while wt (PID-LOAN-6) uses **utilized** exposure — both as specified; column names are carried explicitly so the two are never conflated. The Fed states only that the spread is "the difference between the balance-weighted average interest rate and the base rate" (PDF p. 181) without naming a balance measure — spec §4 |
| PID-LOAN-4 | 2026-08-03 | `ii_loans` (Corporate) | **Spread construction by rate type.** Floating = FloatPoolRate − 3M(PQ0); Fixed = FixedPoolRate − 3M(median origination quarter of the v=1 rows); **Mixed = FixedPoolRate − 3M(median origination quarter of the v=3 rows)** — i.e. Mixed's exposures feed the *Floating* pool while its own spread is built from the *Fixed* pool's rate. A median origination month maps to its calendar quarter (May 2022 → 2022Q2); this is the project's operationalization of Equation A37's `t−a` and **resolves OQ-003 for project implementation**. **Amended 2026-08-03 (user-directed):** the MEV sheet supplies the 3M Treasury back to **1976 Q1**, and where a segment's median-origination base rate cannot be found the base rate **defaults to 0**. Because coverage reaches 1976 Q1, a miss almost always means a missing or unparseable origination date rather than a gap in the rate series; the two causes are counted separately. Note the fallback is not neutral — at a 0 base rate the spread collapses to the full pool rate, so that segment's projected new-origination rate is `3M(t) + FixedPoolRate`, overstated by roughly the omitted base-rate level. Implemented as directed but **never silently**: a per-run census names every affected segment, its cause, and its exposure — spec §5, §8 |
| PID-LOAN-5 | 2026-08-03 | `ii_loans` (Corporate) | **Engine assignment, and rate types 0 and 4 are balance-only.** Variable engine (`IR(t) = max(3M(t) + spread, floor)`) for {2 Floating, 3 Mixed}; Equation A34/A38 fixed engine for {1 Fixed}. Mixed projects as variable — wt is scoped to the Fixed balance so Mixed has no re-origination weight, and the Fed independently treats mixed as variable-rate. **`0` DO NOT USE and `4` Entry Fee Based count toward the total portfolio balance (entering the share denominator and reducing every other segment's share) but are never incorporated into fixed or variable income — they contribute zero.** Half matches the Fed (fee-only earns no income), half diverges (the Fed *excludes* fee-only balances from the total-balances calculation, PDF p. 176) — the divergence **lowers** projected income. Supersedes two readings taken during elicitation (fee-based inheriting the Floating rate; v=0 income = Σ(Utilized × Floor)); neither flows into income — spec §6.1, §6.4, §7 |
| PID-LOAN-6 | 2026-08-03 | `ii_loans` (Corporate) | **Re-origination weight from contractual maturities.** wt(category, LOCOM, PQx) = Σ `Utilized Exposure Global` for reference-key-matching facilities whose `Maturity Date` falls in PQx, ÷ the launch-point **Fixed** balance for that category × LOCOM (HFI: `Outstanding Balance`; HFS: `Value`). **Resolves OQ-001 for project implementation** — no Wholesale credit-loss model output is consumed. **Divergence:** the Fed derives wt from "the default rate, prepayment rate, and maturity rate" (PDF p. 183); this uses maturity only. Note the Fed's own Wholesale limitation says using "actual maturity dates and facility-specific probabilities of default would increase precision" (PDF p. 186), implying the Board does *not* use actual maturity dates — so this is more granular on maturity and silent on the other two components. Guards: wt ≤ 1 per quarter (else Eq A38 is non-convex), and a first-run diagnostic comparing the denominator column against Σ Utilized for the same rows — spec §6.2 |
| PID-LOAN-7 | 2026-08-03 | `ii_loans` (Corporate) | **Interest-rate floors from CORP H.1.** Floor values are a per-facility H.1 column; `NA`, `[NULL]`, and `NONE` mean **no floor**, while a populated `0` is a genuine zero floor. **Resolves OQ-002 for project implementation** (the Fed states floors bind but names no source or values, PDF p. 180). Floors are applied after rate projection, and after blending for the fixed engine. Since H.1 floors are per facility while the model projects one rate per segment, the working collapse is a **balance-weighted floor per segment with within-segment dispersion logged** — flagged, awaiting confirmation. A per-run floor census by source and bind-frequency is required, following the PID-SEC-18 lesson where a literal zero in a floor column silently overrode a populated floor — spec §6, §8, §9 |
| PID-LOAN-8 | 2026-08-03 | `ii_loans` (Corporate) | **Corporate input contract.** One workbook, multiple sheets: **CORP H.1** (facility-level: `Interest Rate`, `Committed Exposure Global`, `Utilized Exposure Global`, `Interest Rate Floor`, `Maturity Date`, origination date, mapping columns), **H.1 mapping** (the three-part key), **M.1 Balance** (portfolio balance — the Eq A32 multiplicand), **FRB Scalars** (Table A8, applied constant every quarter), **MEVs** (3M Treasury projection **and history**), **FR Y-9C** (launch-point reconciliation). Sheet names and paths are company-local config; only the logical contract is committed. Rate and money unit scales are **TO_BE_CONFIRMED and refuse to run** (D-006 discipline) — never inferred from magnitude — spec §2 |
| PID-LOAN-9 | 2026-08-07 | `ii_loans` (Corporate) | **Physical reference key and its collapses** (user-supplied workbook layout + "H.1 Mapping" sheet). CORP H.1 rows carry three columns — `Line Reported on FR Y9C` (H.1 code 1–11), `Interest Rate Variablility` (sic, the sheet's spelling), `Lower of Cost or Market Flag` — composed as `{H.1 code}_{variable type}_{LOCOM}`. Two collapses the model must apply: **H.1 code → Fed Category is many-to-one** (codes 4,5 → Category 1 Commercial and industrial; codes 1,2,7 → Category 8 Loans to Financial Institutions; 10→2, 8→3, 9→4, 6→5, 11→6, 3→7), and the **LOCOM flag is three-valued, collapsing to two**: 3 = HFI, while 1 (LOCOM/HFS) and 2 (FVO) both roll into the Fed's FVO/HFS class (PDF p. 175). **`DO NOT USE` appears in the data as the literal string `[NULL]`** — the user verified the column contains only 1, 2, 3, 4 and `[NULL]`, never a literal 0 and never blank, so a 0 or a blank is an anomaly and is refused rather than mapped. The mapping sheet labels code 4 "Entirely Fee base" (not "Entry Fee Base"), which reads closer to the Fed's "fee-only loans" — spec §3 |
| PID-LOAN-10 | 2026-08-07 | `ii_loans` (Corporate) | **Fed Categories 9, 10 and 11 are one merged bucket sourced from FR Y-9C, priced off the depository-institutions Floating pool** (user-confirmed 2026-08-07). Those three categories carry **no H.1 code and no reference key** — a direct physical confirmation of the Fed's statement that they "have no loan-level data on the FR Y-14Q H.1 schedule" (PDF p. 176). Balances come from the workbook's `FR-Y9C 4Q 2024` sheet: `ID_RSSD` on row 8 identifies the column set, and the values for **BHCK1545** (loans for purchasing/carrying securities) and **BHDM1420** (loans secured by farmland) are read from the third column, reported in **thousands**. The three are **merged into a single bucket** and one rate applied. Rate and spread are taken from the **Loans to Depository Institutions, Floating** pool. **Resolves OQ-037 for project implementation:** the Fed's "their variable-rate lending to depository institutions" is the H.1 code **1 and 2** slice (US depository institutions + foreign banks) — strictly narrower than Fed Category 8, which also contains code 7 (nondepository financial institutions), exactly as the terminology gap predicted. Independently **corroborates PID-LOAN-1**: the sheet marks both MDRMs Floating — spec §2, §5 |
| PID-LOAN-11 | 2026-08-07 | `ii_loans` (Corporate) | **Industry scalar source = the FRB column** (user-confirmed 2026-08-07: "we use column D" of the `FRB SCALARS` sheet). Its seven values match **Table A8** exactly (Auto 0.865, C&I/noncore SME 1.033, Credit Card 0.969, Domestic CRE 1.081, Mortgage 1.014, Noncore 1.072, Rest of wholesale 1.113 — PDF p. 220), so the run uses the Federal Reserve's published scalars. The sheet carries a second, firm-computed scalar column that differs for Domestic CRE (1.199 vs 1.081); it is **not** used, and its header text — which names the institution — stays in the gitignored local config and never enters this repository. **Amended 2026-08-07 (user-directed): the scalars are taken from Table A8 directly and the sheet is no longer a workbook input** — the seven published values are identical to column D, so reading them from the source removes both a parsing dependency and a chance to drift. **Still open (OQ-010):** Table A8 has seven *portfolio* rows while the model works in eleven *Fed Categories*, and the source states no correspondence (footnote 63 even lists eight categories against the table's seven — SQ-11). The project's reading is Category 1 → "C&I, noncore SME loan and card" and everything else → "Rest of wholesale", **except Category 2 (Domestic owner-occupied CRE) → "Domestic CRE"**, which is the one genuinely uncertain assignment: it is domestic CRE by name, but the Fed's "Domestic CRE" row may be meant for the separate CRE model's non-owner-occupied portfolios. Category 6 (International Owner-Occupied CRE) cannot take a *domestic* row at all. The mapping is [INT], overridable per category, and **every run warns on any assignment the source does not support** rather than presenting it as fact — spec §2, §6.3 |
| PID-OB-5 | 2026-07-20 | `ie_other_borrowing` | **Nine-quarter cumulative calibration of α_b (supersedes PID-OB-1/PID-OB-3).** ImpliedOB(b,q) = FRBTotal(b,q) − the four sibling interest-expense model expenses, q = 1…9; α_b solves Σ_q B(b,q)·(R0(b,q)+α_b)/4 = Σ_q ImpliedOB(b,q) with R0 the pre-α Eq A53 rate — closed form α_b = (4·ΣImplied − ΣB·R0)/ΣB (annualized decimal); PQ0 actuals never used; betas unchanged; one constant α_b, no floor/cap, no numerical optimization; Σ balances zero/invalid → validation error; α_b a project calibration parameter, never a Fed coefficient; both quarterly paths kept as diagnostics — chapter §9; resolves the OB part of OQ-009; opens OQ-023 |

## Open questions

### OQ-001 — Loan re-origination weight `wt`: upstream inputs unspecified
**Status: RESOLVED FOR PROJECT IMPLEMENTATION for Corporate (2026-08-03) via PID-LOAN-6 — OPEN for CRE and Retail; the source-side gap is preserved.** For Corporate, wt is derived from contractual `Maturity Date` values already present in CORP H.1 (Σ utilized exposure maturing in the quarter ÷ launch-point Fixed balance), so **no Retail/Wholesale credit-loss model output is consumed** and the external dependency does not block Corporate. Recorded divergence: the Fed derives wt from default, prepayment **and** maturity rates; the project uses maturity alone. The original filing stands for the remaining portfolios:
**Status (original): OPEN (external dependency).** Equation A38's weight wt(p,i,t) "is derived from the default rate, prepayment rate, and maturity rate" and the model uses "estimated loss rates from the Retail and Wholesale models" (PDF pp. 174, 183; md sec-151, sec-165). Those rates, their granularity, and their delivery format are defined outside this document. Resolves: obtain the Retail/Wholesale credit-loss model documentation for the proposed suite, or record the interface as an explicit external input contract.

### OQ-002 — Loan interest-rate floors: values and data source unstated
**Status: RESOLVED FOR PROJECT IMPLEMENTATION for Corporate (2026-08-03) via PID-LOAN-7 — OPEN for CRE and Retail; the source-side absence is preserved.** Floor values are a per-facility column in CORP H.1; `NA`, `[NULL]`, `NONE` mean no floor, a populated `0` is a genuine zero floor. Per-facility floors are collapsed to the segment by a balance-weighted average with dispersion logged (flagged, awaiting confirmation). The original filing stands for the remaining portfolios and for the source-side question:
**Status (original): OPEN.** "All portfolios have a portfolio-specific interest rate floor that will bind if the projected interest rate decreases to the stated floor" (PDF p. 180; md sec-161); Question A153 discusses floor segmentation (PDF p. 187). Where floor values come from (contract data field? assumption?) is UNKNOWN. Resolves: Fed clarification or final-rule documentation.

### OQ-003 — Wholesale spread base rate at "median origination date (t−a)": computation detail
**Status: RESOLVED FOR PROJECT IMPLEMENTATION for Corporate (2026-08-03) via PID-LOAN-4 — OPEN for CRE; the source-side gap is preserved.** The median origination **month** maps to its calendar quarter and that quarter's 3M Treasury is used (May 2022 → 2022Q2), computed per rate type within each Fed Category × LOCOM. Consequence: the 3M Treasury series must extend historically to the earliest median origination quarter across all segments — supplied from **1976 Q1**, with a **base rate = 0 fallback** where a lookup misses (PID-LOAN-4 amendment, 2026-08-03), censused per run because the fallback inflates that segment's new-origination rate by the omitted base-rate level. How the Fed itself measures `a` remains unstated. The original filing stands:
**Status (original): OPEN.** Equation A37 uses the base rate at the median origination date for the portfolio (PDF pp. 182–183; md sec-165). How `a` is measured (median age across loans? balance-weighted?) and at what granularity is not stated. Resolves: Fed clarification; cf. Question A155 (Fed itself asks whether wholesale should switch to the retail approach).

### OQ-004 — Securities Model Description is a separate, uncollected source
**Status: RESOLVED (2026-07-23) — source collected, delegated passages located, extracted, and incorporated; successors OQ-025/OQ-026.** Reinvestment assumptions and the Agency RMBS vendor prepayment model live in the "Securities Model Description" (footnotes 64–65, PDF pp. 193, 196; md 5356, 5358) — a different public document not in `sources/`. Until acquired, reinvestment mechanics and vendor-model behavior are UNKNOWN in the securities chapters (#3–#5). Resolves: user decision whether to add that public document as a second source in `sources/fed/`.
**Narrowing (2026-07-23, user-approved at the asset-side roadmap decision):** the document was collected as `sources/fed/market-risk-models.pdf` — *Supervisory Stress Test Model Documentation: Market Risk Models*, October 2025, **Updated January 2026** (SHA-256 `7e9f633b927d7d0b0d1c8137c3d5a77e942851e975fadf7a0a5fd5c562727770`; source: https://www.federalreserve.gov/supervisionreg/files/market-risk-models.pdf, linked from the Board's 2026 DFA stress-test model-documentation page). Identification [INT, strong basis]: **Section A "Securities Model" (pp. 7–77)** is the referenced description — the running page header reads "Model Documentation: Securities Model"; no standalone securities volume exists in the 2026 documentation series; the section documents the Agency MBS third-party vendor model (p. 10, verified against the page image); and the PPNR source's own footnote 52 cross-references the same volume for the Yield Curve Model.
**Resolution (2026-07-23, asset-side Increment 2):** the delegated passages were located, image-verified, and extracted into `sources/fed/market-risk-models-securities-extracts.md` (scoped working copy; integrity addendum §11): the **Reinvestment Methodology** (MRM pp. 72–74 — hypothetical one-year Treasury purchased at face value, coupon = par Treasury curve yield at the forecast quarter, AFS/HTM intent preserved, no hedges on reinvestments, with an **explicit cross-reference to "the proposed structural model for interest income on securities"**) and the **Agency MBS vendor model** (MRM pp. 18–20 — Monte Carlo, itemized macro inputs, default calibration). Incorporated into securities chapters #3–#5 via `asset-side-common-conventions.md` §12. Residual questions moved to their own entries: **OQ-025** (reinvestment income mechanics) and **OQ-026** (vendor income composition).

### OQ-005 — Hedge treatment: two data states and unclear division of responsibility
**Status: OPEN.** Securities models embed a Hedge Income term that "will be zero" until proposed FR Y-14Q B.2/B.3 revisions are adopted (PDF pp. 192, 197, 203); loans exclude hedges entirely (Question A159, PDF p. 188); Section v.c defines the cross-cutting adjustment contingent on the same proposed collection (PDF pp. 220–225). Unstated: if the collection proceeds, whether hedge effects flow through the embedded per-security terms, the cross-cutting adjustment, or both without double counting. Handbook approach: present both data states per chapter and treat the allocation as UNKNOWN. **Liability-side integration note (2026-07-17):** all five interest-expense chapters (`ie_dom_time_dep`, `ie_other_dom_dep`, `ie_foreign_dep`, `ie_fed_funds_repo`, `ie_other_borrowing`) expose their expense paths and compute no hedge term in-model; the allocation question remains OPEN (see `handbook/cross-cutting/liability-side-common-conventions.md` §7; `ie_other_borrowing` carries an α_b double-counting caution). Resolves: final data-collection outcome + Fed methodology statement.

### OQ-006 — Rate units and quarterly conversion conventions (global)
**Status: RESOLVED FOR PROJECT IMPLEMENTATION (2026-07-17) — via user-confirmed annualized-rate convention.** Within the proposed 2026 suite, the source states ÷4 annual→quarterly conversion explicitly only for trading NII data construction (PDF p. 225) and inside the securities coupon formulas (Coupon Rate/4; N/360 for hedge legs); current-suite footnote 54 (subordinated debt) separately states "coupon rates are always stated as annualized rates" with an annual-rate ÷4 (PDF p. 155) — supports the annualized-rate resolution; identified in the Step 5 review (2026-07-17). For the deposit-expense models ("multiplying the modeled rate by the average balance", PDF pp. 209, 214), the calculators (Eqs A39, A43, A48), and other-borrowing (Eq A53(1)), whether rates are annualized (requiring ÷4 for a quarterly flow) is not stated — **this source-side absence remains true and is preserved**. Loans state "interest income is quarterly compounded" (assumption 3, PDF p. 184) without defining the rate basis. **Resolution (PROJECT IMPLEMENTATION DECISION — USER CONFIRMED):** all interest rates used in this project are annualized rates; when an annualized projected rate is used to calculate one quarter's interest expense, the annualized rate is divided by four (simple nominal quarterization, not effective compounding). For domestic time deposits: Expense(b,t) = AverageBalance(b,t) × Rate(b,t) / 4, with the Equation A44 recursion remaining entirely in annualized-rate units — division by four occurs only in the final quarterly-dollar expense calculation. The PPNR source itself does not explicitly state the quarterly conversion for this component; the resolution is project implementation context, not a Fed statement. Where the source states its own conversion (securities /4; hedge N/360; trading-NII ÷4), the source statement governs. See `research/source-briefs/interest-expense/deposits/ie_dom_time_dep.source-brief.md` §7.9 (PID-6).

### OQ-007 — Trading NII: ratio-to-dollar projection step implied, not stated
**Status: OPEN (working interpretation per D-002 spirit).** Equation A52 projects a Ratio normalized by net trading assets (PDF p. 225); the source never states the projection-side multiplication. INTERPRETATION: projected dollars = Ratio(b,q) × net trading assets at the launch point, constant per the flat-balance convention. Resolves: Fed clarification.

### OQ-008 — WAL units in ρ = 1/WAL (time deposits)
**Status: RESOLVED FOR PROJECT IMPLEMENTATION (2026-07-17) — via user-confirmed FR Y-14Q Schedule G context, not a PPNR-source statement.** Equation A44 defines ρ_b ≡ 1/WAL_b as "the fraction of the portfolio that reprices every period (quarter)" using Schedule G item 71 (PDF p. 209). WAL must be expressed in quarters, but the reporting unit of item 71 is not stated in the PPNR document (and still is not). **Resolution (PROJECT IMPLEMENTATION DECISION — USER CONFIRMED):** item 71 reports WAL in months; WAL_quarters(b) = WAL_months(b) / 3; ρ_b = 3 / WAL_months(b). This resolution is project implementation context; the PPNR model section itself does not state the unit. See `research/source-briefs/interest-expense/deposits/ie_dom_time_dep.source-brief.md` §0.1 (PID-3/PID-4) and §13.

### OQ-009 — Firm fixed effects not disclosed; backsolving mechanics undefined
**Status: OPEN for `nii_trading_al` only — RESOLVED FOR PROJECT IMPLEMENTATION for `ie_other_borrowing` (2026-07-20, via PID-OB-5).** Table A9: "Estimated coefficients for firm fixed-effects are not included in the table" (PDF p. 234; md sec-224); "backsolve" appears nowhere in the source. For `ie_other_borrowing`, PID-OB-5 replaces the launch-point backsolve (PID-OB-1/PID-OB-3, superseded): α_b is calibrated in closed form so the nine-quarter cumulative modeled expense equals the cumulative expense implied by the FRB total-interest-expense path — PQ0 actuals never enter, so the former residual sub-items (the PQ0 actual-expense line item; the PQ0 low-rate-regime mismatch) are moot; the successor open item is OQ-023 (FRB-total physical source). For `nii_trading_al` (#11) all original sub-questions remain open and D-002's launch-point working method (INTERPRETATION) still applies there: recover α_b at the launch point from observed actuals given disclosed macro coefficients. Resolves (for #11): Fed disclosure of fixed effects, or a validated backsolve specification in the coding phase (generic, non-confidential).

### OQ-010 — Table A8 has 7 scalar rows; footnote 63 lists 8 categories
**Status: OPEN.** Footnote 63 (PDF p. 184): mortgage, auto, corporate & investment, small and median business loans and card, domestic CRE, consumer credit card, rest of consumer, rest of wholesale — 8 categories. Table A8 (PDF p. 220) has 7 rows; "C&I, noncore SME loan and card" appears to merge two footnote categories (integrity review SQ-11). Exact category→row mapping (and which portfolios each scalar multiplies) is UNKNOWN. Resolves: Fed clarification or updated table.

### OQ-011 — Other Consumer Products: jump-off rate mapping
**Status: OPEN.** Jump-off rates come from "the firm-level earned interest rates reported in the FR Y-14Q pre-provision net revenue line-item report for the most closely aligned business line" (PDF p. 180; md sec-160). Which G.2 line maps to which product is unstated, as is the "expert judgment" fixed/variable split for non-core (PDF p. 181). Resolves: Fed clarification; interim handbook treatment = UNKNOWN mapping.

### OQ-012 — Credit-card revolver share: constant over the horizon?
**Status: OPEN (minor).** The revolver share is calculated at the segment/firm level and "used as inputs in credit card interest income projection" (PDF p. 179; md sec-159). Nothing states whether it changes over the projection. INTERPRETATION: constant from the launch point, consistent with the model's constancy conventions. Resolves: Fed clarification.

### OQ-013 — ELB boundary at exactly 25 basis points
**Status: OPEN (minor edge case).** ELB is "below 25 basis points", non-ELB "above 25 basis points" (PDF p. 211); Equation A46's condition is written Treasury3m > 25bps (PDF p. 212). The Treasury3m = 25bp case is unassigned. CODING CONSIDERATION: pick one branch and document it; immaterial to methodology. Affects `ie_other_dom_dep` and `ie_foreign_dep` (both use the Equations A45–A47 framework); the working branch (= 25 bp → non-ELB, per the printed A46 condition) is a documented [CODE] choice, not a PID. Resolves: Fed clarification (unlikely needed).

### OQ-014 — Industry scalar application granularity
**Status: OPEN (confirmation).** As proposed, the scalar is a constant multiplicative true-up "by industry for each loan category," applied every projection quarter (PDF p. 184). Question A157 floats bank-level vs. portfolio-level alternatives (PDF p. 188), so the final design may change. Handbook records the as-proposed reading; watch the final rule.

### OQ-015 — Truncated sentence: wholesale segmentation data source
**Status: OPEN (minor; interpretation available).** "Data used in the segmentation of wholesale balances are sourced from FR." (PDF p. 175; md sec-153 — truncation is in the PDF itself, integrity review SQ-5). INTERPRETATION: FR Y-14Q (Schedule H.1), per footnote 61 and the Corporate section's references. Resolves: Fed correction in a future revision.

### OQ-016 — Other-domestic-deposit balance line-item mappings unnamed in source
**Status: RESOLVED FOR PROJECT IMPLEMENTATION (2026-07-17) — via PID-ODD-1/PID-ODD-2 (user-confirmed).** Neither the Eq A47 subcomponent balances ("the balance reported in the Y-14Q corresponding to the rate i", PDF p. 213; md sec-198) nor the expense-step "average balance on other domestic deposits as reported in the FR Y-14Q" (PDF p. 214; md sec-198) has a named line item — a preserved [FACT] absence. Resolution (project context, never a Fed statement): subcomponent balances = Schedule G items 34B/34C/34D (PID-ODD-1); total average balance = MDRM sum BHCB3187 + BHOD3187 + BHCB2389 + BHOD2389 (PID-ODD-2). The two mappings use different physical sources; their closeness is a validation monitor, not an identity (`ie_other_dom_dep` §9/§10). **Revised 2026-07-23 (PID-ODD-3):** the expense multiplicand is the Σ(34B/34C/34D) weight sum itself — confirmed by alignment against the company reference implementation — and the PID-ODD-2 MDRM sum is retained only as the optional consistency-monitor reference.

### OQ-017 — Spread(i,b) estimation mechanics underspecified
**Status: OPEN.** Affects `ie_other_dom_dep` and `ie_foreign_dep`. The estimation sentence is truncated in the PDF (SQ-15, p. 212; md sec-198): the comparator "…and the 3-month Treasury yield" is elided (recoverable [INT] from the Eq A45 where-list). Also unstated: sign convention (signed vs. absolute distance; interim [INT]: signed difference rate − Treasury3m), averaging frequency over 2020:Q2–2021:Q4, and treatment of firms lacking that history (no fallback stated — failures surface). Resolves: Fed clarification or final-rule text.

### OQ-018 — Recursion seed timing at t = 1 (Equations A45–A47 models)
**Status: OPEN (minor; interpretation recorded).** Affects `ie_other_dom_dep` and `ie_foreign_dep`. The source retrieves launch-point rates (42B–42D; 43A/44B) but never states that they seed Rate(i,b,t−1) at t = 1, nor that ΔTreasury3m(1) uses the jump-off value — both are [INT] (chapters §4/§6; foreign INT-c/INT-f). Resolves: Fed clarification.

### OQ-019 — Line item 44B used as both a rate item and a balance item
**Status: OPEN (form-instructions cross-check pending).** v.a(9) names 44B as the "foreign deposits-time" *rate* item (PDF p. 215; md sec-202); v.a(10) names 44A/44B as "the liabilities reported" — balances (PDF pp. 216–217; md sec-205). Logged as quirk SQ-16. Direction indicated by PID-FFR-1 (user-confirmed: 44A/44B are rate items; v.a(10)'s balance usage is the misname; physical balances = 36A/36B). Resolves: FR Y-14Q Schedule G instructions cross-check in the coding phase; final-rule correction.

### OQ-020 — Foreign beta items 83A/83B/84A/84B: subcomponent/direction mapping unstated
**Status: OPEN (minor).** The source lists the four items flat (PDF p. 215; md sec-202); which is (non-time vs. time) × (up vs. down) is unstated. Interim [INT-b]: 83A/83B = non-time up/down, 84A/84B = time up/down, by ordering parallel with 79A–81B. Immaterial to output (the model uses Table A7 medians); matters only for the Table A7 reproduction validation. Resolves: form instructions or Fed clarification.

### OQ-021 — Deposit betas: hard-code Table A7 or recompute medians at lift-off
**Status: OPEN (minor).** Affects `ie_other_dom_dep` and `ie_foreign_dep`. "The beta used in the model is the median of the firm-reported betas for the respective deposit category at lift-off" (PDF p. 213; md sec-198) implies recomputation each cycle, while Table A7 (PDF p. 219; md sec-209) publishes specific values for the 2026 proposal. Interim handbook treatment: Table A7 values are the supplied parameters; the recomputation question is recorded. Resolves: final-rule practice.

### OQ-022 — Other-borrowing share denominators: mixed FR Y-9C / Schedule G form
**Status: OPEN.** Affects `ie_other_borrowing` (formerly component-scoped placeholder OQ-OB-A). Share numerators are FR Y-9C MDRMs (BHCK2309; BHDM4062 + BHDMC699 — PDF p. 232; md sec-221) while the total balance is a Schedule G sum; the projection-side physical form of the shares is unstated. Literal mixed reading recorded as [INT]; shares validated ∈ [0,1], sum ≤ 1, violations surface, never clipped. Resolves: Fed clarification or a user-confirmed mapping in the approved environment.

### OQ-023 — FRB total-interest-expense path: physical source and scope alignment
**Status: OPEN — narrowed 2026-07-20 (user-stated).** Affects `ie_other_borrowing`. The PID-OB-5 calibration consumes a per-firm, per-scenario FRB total-interest-expense projection for PQ1–PQ9 (`frb_total_interest_expense`, USD per quarter). The Fed source states no total-interest-expense aggregation for the proposed suite — Section v models each component independently ([FACT] absence) — so this path is a project-supplied input, never a Fed statement. **Narrowing (user-stated):** the FRB provides the firm three projection paths — interest income, total interest expense, and net interest income; `frb_total_interest_expense` maps to the FRB-provided total-interest-expense path, and the income/NII paths are carried alongside as family-level inputs (`frb_total_interest_income`, `frb_net_interest_income`; the NII = income − expense identity is monitored as a [CODE] wiring guard; income is the future asset-side counterpart). Remaining open: the physical file/format mapping of the three paths, and whether the FRB expense total's scope aligns exactly with the five modeled interest-expense components (domestic time, other domestic, foreign, fed funds/repo, other borrowing) so that the implied residual is well-defined. Resolves: a user-confirmed file mapping and scope confirmation in the approved environment.

### OQ-024 — Other interest/dividend-bearing assets: α share derivation mechanics unstated
**Status: OPEN (working treatment recorded). Filed 2026-07-23 (asset-side Increment 1).** Affects `ii_other_ida` (#6). The source states the fed-funds-sold/reverse-repo portion "can be quantified by utilizing the additional fields provided by the firms in the footnotes to the worksheet and by cross-referencing with the balances reported in FR Y-9C (BHCK3365)" (PDF p. 206; md sec-189; page image verified 2026-07-23) — but **which** footnote fields enter the α numerator, and **how** the BHCK3365 cross-reference resolves disagreements between the two sources, is not stated. Project treatment [CODE]: α (`other_ida_short_rate_share`) is a **supplied launch-point input** (decimal share, validated ∈ [0,1], never clipped); its upstream derivation is data preparation outside the model boundary (precedent: the deposit-expense Spread(i,b) supplied-input treatment). Resolves: Fed clarification of the derivation, or a user-confirmed physical mapping (candidate PID) in the approved environment.

### OQ-025 — Securities reinvestment: income-side mechanics residuals
**Status: LARGELY RESOLVED FOR PROJECT IMPLEMENTATION — items (a), (b), (d) user-confirmed 2026-07-23; (c) pending the positions-format upload; (e) awaits the final rule. Filed 2026-07-23 (asset-side Increment 2).** Affects `ii_ust` (#3), `ii_mbs` (#4), `ii_other_sec` (#5). The reinvestment instrument is now sourced (OQ-004 resolution; MRM pp. 72–74), but five income-side details remain unstated: **(a)** RESOLVED FOR PROJECT IMPLEMENTATION (user-confirmed 2026-07-23): a one-year reinvestment that matures inside the horizon **rolls again** — and the reinvestment's coupon is **fixed at the purchase-quarter 1Y Treasury yield for its full four-quarter window**, resetting to the then-current 1Y yield at each roll; **(b)** RESOLVED FOR PROJECT IMPLEMENTATION (user-confirmed 2026-07-23): reinvestment income **stays attributed to the originating component** (nothing migrates to `ii_ust`); the uniform-into-Treasuries reading of MRM p. 73 stands as the instrument assumption; **(c)** RESOLVED FOR PROJECT IMPLEMENTATION (2026-07-24): paydown proceeds **reinvest like maturities** — MRM p. 72 states the reinvestment covers balances "maturing or decreasing in balance due to partial paydowns" [FACT]; purchase on the first day of the following quarter mirrors the maturity rule [INT]; implemented with a `reinvest_paydowns` config toggle for A/B against the company reference (motivated by the 2026-07-24 comparison: `ii_mbs` was ≈37 % low without it); **(d)** RESOLVED FOR PROJECT IMPLEMENTATION (user-confirmed 2026-07-23): the project's `usd_1y_treasury` MEV **is** the par-Treasury-curve 1-year yield — it serves as the reinvestment coupon series (never presented as a Fed statement about the MEV file); **(e)** the passage sits inside MRM "Alternative Approaches" and says "The **current** reinvestment assumption is…" — the final-rule adoption status of the alternative framework is open (MRM Question A1 solicits comment). Resolves: Fed final-rule documentation; user-confirmed mappings at the coding phase.

### OQ-026 — Agency RMBS vendor income: composition and black-box boundary
**Status: RESOLVED FOR PROJECT IMPLEMENTATION (2026-07-23, via PID-MBS-1) — the Fed-side absence is preserved as [FACT].** The user confirmed the vendor input's semantics: a per-security **remaining-balance (survival) factor** path (PQ0..PQ9, PQ0 ≡ 1) from the company "prepayment" sheet; Face(i,q) = factor(i,q) × Face(i,0) and income is computed by the A41 terms on that factor-driven face (coupon accrual per PID-SEC-4). The vendor model itself remains a black box behind the supplied factor path.
Original filing (retained): Affects `ii_mbs` (#4). v.a(4) states "a vendor model is used to calculate the income to better reflect the impacts of prepayments" (PDF p. 196) and separately assigns Agency RMBS the straight-line/WAL accretion description (PDF p. 197) — **how the vendor output composes with Equation A41's three terms is not stated** (replaces the whole sum? only coupon accrual? supplies paydown paths?). The vendor model itself is a third-party black box (Monte Carlo over macro-conditional paths; itemized inputs: mortgage rate, prime, unemployment, HPI, zero-coupon Treasury and SOFR curves, MBS/CMO OAS auxiliary indices; "the vendor's default model calibration without any adjustment or overlay" — MRM pp. 18–19) that the project cannot and does not reimplement. **Working treatment [CODE, candidate PID-MBS-1]:** the Agency RMBS category's pre-hedge income enters as a **declared quarterly input path** supplied by the firm (its vendor-equivalent output); paydown/reinvestment interaction for this category rides inside that path unless the user's data says otherwise. Resolves: Fed clarification, or a user-confirmed vendor-output mapping in the approved environment.

### OQ-027 — U.S. Treasuries: missing vendor-coupon fallback unstated
**Status: OPEN (minor). Filed 2026-07-23 (asset-side Increment 2).** Affects `ii_ust` (#3). v.a(3) states coupon rates come from vendor data "which in some cases may not be available" (PDF p. 192) and stops there — **no fallback is stated for this component**, while v.a(4) explicitly falls back to the Schedule B.1 book yield (PDF p. 196; page images verified 2026-07-23). Working treatment [CODE]: a missing coupon surfaces as an error; the `ii_mbs` book-yield fallback is **not** silently borrowed. Resolves: Fed clarification or a user-confirmed treatment (candidate PID) at the coding phase.

### OQ-028 — Sovereign zero-coupon bonds: reference workbook internally inconsistent
**Status: OPEN — waiting on the reference workbook's owners (via the user). Filed 2026-07-24 (compare mode).** Affects `ii_other_sec` (#5) verification only — not the model itself. The user's spot-check (2026-07-24): among sovereign zero-coupon rows, the reference II_PQ columns are **internally inconsistent** — some rows are blank (no income at all), some carry income in PQ1 only, and some carry a **constant** amount PQ1–9. Our treatment (PID-SEC-8, uniform): cash coupon 0 + AA = (prior face − prior AC)/(4 × maturity years) every quarter alive — which books income for every ZCB and therefore shows as OURS-HIGH ~100 % against the blank/partial rows (the Sovereign Bond aggregate ran ≈ +8 % on the xr basis). CODING CONSIDERATION: the compare mode now reports a per-category `ref-zero` count so these rows are visible instead of polluting the band statistics; the model is **not** being bent toward an inconsistent target. Resolves: the user asks the workbook's owners which of the three reference behaviors is intended for ZCBs (and whether blanks mean "booked elsewhere"); a PID records the answer.

### OQ-030 — Municipal Bond residual: the AA leg (book-yield adjustment hypothesis)
**Status: RESOLVED 2026-07-28 via PID-SEC-14 — no formula read was needed in the end; the per-security gaps CSV settled it.** The reference computes municipals by the **printed Equation A42**: income(q) = AmortizedCost × BookYield ÷ 4, constant across all nine quarters, with no separate accretion leg. Verified on 20 rows spanning AC/face 0.774–1.193 (including premium rows where AC > face) to within book-yield display rounding; the same amount against FACE is out by up to 29 %, which is why every earlier face-based candidate (`by_face_only` = 1.0878, `by_face_aa` = 1.1392) missed. The "tax-equivalent gross-up" hypothesis below is **withdrawn** — the 0.794 percentile was the AC/face ratio showing through a face-based rule, not a 1/(1−21 %) factor. Original filing retained for the audit trail:

**(superseded) Status: OPEN — one formula read decides it. Filed 2026-07-27 (after the per-category convergence run: total xr/ref = 0.9938, Municipal 0.8870 = −335 USD mm is the largest remaining gap).** Affects `ii_other_sec` (#5). Established: muni coupons identical in ITO and main sheet; the Coupon Accrual formula references the ITO coupon; no TEB factor in the formulas; book-yield-as-coupon REFUTED (split population). Remaining evidence: the reference's implied multiplier is **time-constant** (1.131 fixed / 1.189 floating) with a slight UPWARD drift — the signature of a **constant AA** while ours declines (PID-SEC-8 recursion); the muni floaters fit book-yield × face exactly (1.0000); per-row ratios bottom at 0.794 = 1/1.266 — consistent with the Y-14 muni **book yield being reported on a tax-equivalent basis** (the "TEB factor" living in the BY data, not in a formula). HYPOTHESIS: muni AA = (book yield − coupon) × face / 4 (a constant yield adjustment; total income = book yield × face / 4). The compare's FIXED-RULES block now prices `by_face_aa` and `by_face_only` alongside `current`. Resolves: the user reads the muni **AA cell formula** (does it reference book yield / BY − coupon, or (face − AC)/(4 × maturity)?); a PID records the outcome.
**Update 2026-07-27 (user read the formula): AA = (face − AC)/(4 × maturity(yr)) — IDENTICAL to ours; the BY-adjustment hypothesis is refuted too.** With formula, coupon, face, and AC all identical, the only remaining input is the **divisor**: their formula divides by the sheet's own `Maturity (yr)` column; ours divides by the ITO maturity date ÷ 365. Munis are the heavily CALLABLE category — if `Maturity (yr)` is call-adjusted (shorter than final maturity), their AA is larger on discount munis and more negative on premium ones, which matches the whole per-row pattern. Config `maturity_source = "positions_first"` now makes the sheet column the primary AA denominator (ITO date fallback; converged categories should be unaffected where the two agree). Verification: the next compare run; plus a spot check that a muni's `Maturity (yr)` < ITO date-diff/365 (call date vs final maturity).

### OQ-029 — Auction Rate Securities: reference books zero income
**Status: OPEN — waiting on the reference workbook's owners (via the user). Filed 2026-07-27 (compare mode).** Affects `ii_other_sec` (#5) verification only. All 8 ARS rows with references carry II_PQ ≡ 0 (`ref-zero:8`; FLOAT-RULES "ref-total-zero") while our PID-SEC-8 treatment books a small amount (≈ 0.3 USD mm 9Q, coupons now available via the PID-SEC-9 fallback). CODING CONSIDERATION: immaterial to totals; do not zero a category to match an unexplained blank. Resolves: the owners confirm whether ARS are deliberately excluded (failed-auction/frozen assumption?) — a PID records the answer.

### OQ-031 — Blank amortized cost: is the reference's zero intended?

**Status: OPEN — waiting on the reference workbook's owners (via the user). Filed 2026-07-28 (PID-SEC-13).** Affects every securities component; found on an Agency MBS row (250.00 face, 2.5 % coupon, WAL 9.6556y, price 83.445) whose Schedule B.1 amortized cost is **blank**. The reference reads the blank as **zero** and accretes the entire face over 4 × WAL — 63.5927 of 9Q income on a 250 face, ≈ 21 % cumulative on a 2.5 % coupon bond, of which 52.57 is accretion. That is arithmetically consistent with the workbook's own formula but is not a defensible carrying value, so `zero_accrete` is offered as a **switch** (PID-SEC-13) and left OFF by default rather than adopted. Two things to establish: (a) is the amortized cost genuinely missing at source for these rows, or is it available in a field the extract does not carry; (b) do the owners intend blank ⇒ 0, or is the blank an extract defect the workbook silently inherits? CODING CONSIDERATION: the run's PID-SEC-13 census reports how many rows are affected and how many can be classified as near-settle — if the population is small, the totals impact is bounded whichever way (a) resolves. Resolves: the owners answer; a PID records it and fixes the default.

### OQ-032 — Maturity-quarter rounding: is the split by income form rather than category?

**Status: MECHANISM RE-INSTATED 2026-07-29 (the 2026-07-28 refutation was my over-reading).** The user's statement that the A42 cell points at a **static** `AMORTIZED_COST` constrains the MULTIPLIER, not the maturity GATE — a cell of the form `IF(this quarter's face > 0, AMORTIZED_COST × book yield / 4, 0)` is entirely consistent with it. So the form-based explanation stands, and it is now derivable from the user's own two statements rather than hypothesised:

- **Split form** — Coupon Accrual = coupon/4 × the **PRIOR** quarter's EOP face (user-confirmed). For a security maturing inside PQ4 the EOP faces are F,F,F,F,0,… so PQ4's accrual reads PQ3's face, which is still F, and **PQ4 books a FULL quarter** (no prorated stub — exactly as described). Income runs PQ1..PQ4 = **ceil**(4 × years). Independently confirmed by US Treasuries: ceil gives xr/ref 0.9985, floor gives 0.9766 (one quarter of coupon lost on 181 rows).
- **A42 form** — income is the static AMORTIZED_COST × book yield, and empirically stops one quarter earlier (Sovereign fixed rows reach 1.0006 only under floor). That requires the gate to read the **CURRENT** quarter's balance rather than the prior one.

Both of the user's statements are therefore correct; they simply describe two different cells. Remaining question: what makes an A42 row stop — is there an explicit `IF` on the current quarter's face in that cell? Original filing: The user read both cells. (a) **Coupon Accrual references the PRIOR quarter's face** — that is the split-form leg, and since the prior quarter's face is still positive in the quarter a security matures, the split form books that quarter: **ceil**, now on evidence rather than incumbency. (b) The A42/book-yield cell is `AMORTIZED_COST × book yield` for fixed rows and `AMORTIZED_COST × margin assumption` for floating ones, and it points at the **static AMORTIZED_COST column on the main positions sheet — a single launch-point number, not a quarter-varying column**. That **refutes the proposed mechanism**: floor cannot come from a per-quarter amortized cost going to zero, because there is no per-quarter amortized cost. Something else stops the A42 accrual, one quarter earlier than the coupon leg stops. **Consequence: do NOT auto-derive rounding from `a42_collapsed_categories`** — the empirical split (floor for the A42 categories, ceil elsewhere) stands, but its cause is unexplained, so `maturity_quarters_rounding_overrides` stays an explicit, evidenced switch. Confirms separately that PID-SEC-14's implementation is right: it already uses the static launch amortized cost, constant across the horizon. New sub-question — what column is the **"margin assumption"** used for FLOATING rows in an A42 category? Book yield reproduces the five Municipal floaters to within 0.1 % (all 1992 muni rows land inside 0.1 %) and Sovereign has essentially no floaters, so nothing is mis-stated today; it would matter before putting any floater-heavy category on the A42 form. Original filing: That is the split-form leg: the prior quarter's face is still positive in the quarter a security matures, so that quarter DOES book and the split form takes **ceil**. `ceil` therefore stands as the global default on evidence, not merely as the incumbent. Outstanding: whether the A42/book-yield leg reads the CURRENT quarter's amortized cost (which would give **floor** for A42 categories and complete the form-based explanation). Original filing: `floor` reproduces the reference for Sovereign Bond (fixed rows to xr/ref 1.0006) but costs US Treasuries 544 USD mm — one quarter of coupon on 181 rows. Rather than two categories genuinely disagreeing, the likely explanation is that the two income forms read different columns: the **A42 collapsed form** (income = BookYield × amortized cost ÷ 4) takes the CURRENT quarter's amortized cost, which is zeroed in the maturing quarter, so the maturing quarter books nothing (**floor**); the **split coupon+AA form** takes the PRIOR quarter's EOP face (PID-SEC-4), which is still positive in the maturing quarter, so that quarter does book (**ceil**). Resolves: the user checks whether the workbook's Coupon Accrual cell references the prior quarter's face column while the A42/book-yield cell references the current quarter's amortized cost. If confirmed, `maturity_quarters_rounding` stops being a free per-category switch and becomes a consequence of `a42_collapsed_categories`, which removes a way to get it wrong.

### OQ-033 — Loans fixed-rate machinery: firm dimension of Equations A34/A35/A36/A38

**Status: OPEN. Filed 2026-07-30 (loans workstream slice 1).** Affects `ii_loans` (#1). The printed subscripts of Equations A34 (IR_(existing,t)), A35 (IR_(p,i,t, new orig)), A36 (Spread_(p,i,t=0)), and A38 (IR_existing,(p,i,t); weight wt_(p,i,t)) **omit the firm dimension b**, while Equations A33 and A37 include it and the fixed-rate prose states "the Board proposes to use the balance-weighted origination rate **by firm, product, and segment** at jump-off" (PDF pp. 182–183; md sec-165; quirk SQ-18). Unclear whether the fixed-rate spread (A36 retail branch), the new-origination rate (A35), and the re-origination weight wt operate per firm or pooled across firms. Working reading [INT, recorded in `handbook/models/ii_loans_common.source-brief.md` §5.1/§7.6]: firm-level throughout — the prose and the A33/A37 pattern support it; the subscript omission is notational abbreviation. The wholesale spread itself is unaffected (A37 carries b). Resolves: Fed clarification or final-rule text.

### OQ-034 — Corporate segment grid: "16 out of 22" derivation and segment total unstated

**Status: OPEN. Filed 2026-07-30 (loans workstream slice 1).** Affects `ii_loans` (#1, Corporate). "Most of the firm's corporate portfolios (16 out of 22) are further segmented by the interest rate variability" (PDF p. 175; md sec-154). The source does not derive the 22, does not name which cells split, and states no corporate segment total — in contrast to CRE's stated 24 segments (PDF p. 177; md sec-155). [INT, recorded in `handbook/models/ii_loans_wholesale.source-brief.md` §2.2]: 22 = 11 portfolios × 2 asset classifications (HFI; FVO/HFS); the 6 unsplit cells are the 3 portfolios without FR Y-14Q H.1 loan-level data (loans for purchasing and carrying securities; domestic farmland; international farmland) × 2 classifications; the implied corporate segment count is 16 × 2 + 6 = 38. Resolves: Fed clarification, an FR Y-14Q H.1 structure cross-check at the coding phase, or final-rule text.

### OQ-035 — Mixed-rate/demand-loan and fee-only rules: stated under Corporate; CRE applicability unstated

**Status: OPEN. Filed 2026-07-30 (loans workstream slice 1).** Affects `ii_loans` (#1, Wholesale). The rules "The interest income model treats mixed-rate and demand loans (loans where the lender can demand full repayment at any time) as variable-rate" and "Fee-only loans are assumed to generate no interest income. Hence, they are not used to calculate the average interest rate, and their outstanding balance percentages are excluded from the total balances calculation" appear inside the **Corporate** subsection only (PDF p. 176; md sec-154). The CRE subsection says its segmentation "is similar to Corporate" without restating them (PDF pp. 176–177; md sec-155). Working assumption (flagged in `handbook/models/ii_loans_wholesale.source-brief.md` §4, never source-attributed): the rules are wholesale-wide, since mixed-rate/demand/fee-only are facility attributes not specific to corporate lending. Formally UNKNOWN for CRE until resolved. Resolves: Fed clarification or final-rule text.

### OQ-036 — Farmland loans: rate type unstated

**Status: RESOLVED FOR PROJECT IMPLEMENTATION (2026-08-03) — via PID-LOAN-1 (user-confirmed); the source-side absence is preserved as [FACT].** Filed and resolved the same day (loans workstream slice 2, Corporate). Affects `ii_loans` (#1, Corporate). Three Corporate portfolios are "not segmented by interest rate variability because they have no loan-level data on the FR Y-14Q H.1 schedule" — loans for purchasing and carrying securities, domestic farmland loans, and international farmland loans (PDF p. 176; md sec-154). All three share the NPML proxy spread, and all three are named in the p. 186 limitation restatement. But the rate-type conclusion names **only one**: "The majority of NPMLs were variable-rate, so the Board assumes **loans for purchasing and carrying securities** have variable rates" (page image verified 2026-08-03). No sentence assigns a rate type to domestic or international farmland loans, yet an unsegmented portfolio still requires one rate engine or the other. **Resolution (PROJECT IMPLEMENTATION DECISION — USER CONFIRMED 2026-08-03, PID-LOAN-1):** all three data-limited portfolios are treated as **floating (variable) rate**, each projected as a single undivided block on the Equation A33 path. Supporting reasoning recorded in `handbook/models/ii_loans_corporate.source-brief.md` §6.4: the borrowed proxy spread is drawn from *variable-rate* lending and a "base rate + constant spread" construct is the variable-rate form (a fixed-rate treatment would instead require the Eq A37 wholesale spread, so mixing them would be internally incoherent); the p. 186 limitation groups all three under one NPML proxy; and wholesale balances are majority variable-rate. A countervailing consideration is recorded, not resolved: farmland and agricultural real-estate lending commonly carries a substantial fixed or long-reset component, so a fully-repricing treatment may overstate those balances' scenario sensitivity — materiality depends on firm balances and is UNKNOWN at handbook stage. **The source-side absence remains true and on record:** the Federal Reserve states a rate type for loans for purchasing and carrying securities only. Resolves on the source side: Fed clarification or final-rule text.

### OQ-037 — NPML proxy spread: data slice and granularity

**Status: RESOLVED FOR PROJECT IMPLEMENTATION (2026-08-07) — via PID-LOAN-10 (user-confirmed); the source-side ambiguity is preserved.** The firm's workbook carries a separate "Loans to Depository Institutions" grouping restricted to **H.1 codes 1 and 2** (US depository institutions and foreign banks), excluding code 7 (nondepository financial institutions) — so the Fed's "their variable-rate lending to depository institutions" is that narrower slice, strictly inside Fed Category 8 rather than equal to it, exactly as the terminology gap below predicted. The three data-limited categories are merged into one bucket and priced off that pool's **Floating** rate and spread. Sub-question (b) — how a bank-level spread enters a (b,p,i) rate — is answered in practice by the merge: one bucket, one rate. **The Federal Reserve's own text remains ambiguous**, and this resolution is project implementation context, never a Fed statement. Original filing retained:

**Status (original): OPEN. Filed 2026-08-03 (loans workstream slice 2, Corporate).** Affects `ii_loans` (#1, Corporate). "The Board assumes, for these portfolios, the same **bank-level** interest rate spread as reported in their variable-rate lending to depository institutions" (PDF p. 176; md sec-154). Two unknowns: **(a)** which data slice "variable-rate lending to depository institutions" denotes — "depository institutions" is narrower than Corporate portfolio (8) "loans to financial institutions", so the phrase may mean a sub-slice of that portfolio, the whole of it, or a dataset outside the 11-portfolio census; the source never links the two terms, and the only other Section v occurrence of "depository institutions" (PDF p. 190) belongs to the deposits-with-banks model and is unrelated (verified 2026-08-03). **(b)** How a **bank-level** spread enters Equation A33's Spread(b,p,i,t=0) — assigned unchanged to every affected cell, or aggregated/weighted first — is unstated. This is a stated granularity exception: every other spread in the model "varies by firm, product, and segment" (PDF p. 181). Working reading [INT, flagged]: taken from the variable-rate portion of the firm's lending to depository institutions and applied unchanged to each affected cell. Resolves: Fed clarification, or a user-confirmed mapping (candidate PID) in the approved environment.

### OQ-038 — Corporate disclosure categories: defining form unstated; residual categories undefined

**Status: OPEN. Filed 2026-08-03 (loans workstream slice 2, Corporate).** Affects `ii_loans` (#1, Corporate). The Corporate section defines its scope extensionally — "segmented by 11 disclosure categories and loan types (referenced as a portfolio)" followed by the list (PDF p. 175; md sec-154) — and names **no form or schedule** that defines those categories. The contrast is explicit and on the facing page: CRE's six loan types are "as defined in FR Y-9C" (PDF p. 176; md sec-155; both verified on page images 2026-08-03). Two portfolios are also residuals with no definition, inclusion rule, or example anywhere in the document: "other non-consumer loans" (3) and "other leases" (4); relatedly, the source never addresses how **lease** income is treated inside an interest-income model. A non-normative, unverified name-similarity cross-walk to FR Y-9C loan-category language is recorded in `handbook/models/ii_loans_corporate.source-brief.md` §13 (no item numbers or MDRM codes asserted — the FR Y-9C is not in `sources/`). Resolves: Fed clarification, or an FR Y-14Q H.1 / FR Y-9C instructions cross-check at the coding phase.

## Question-numbering note

Board question numbers A151–A191 cover the net-interest scope, but A161/A162 are duplicated across components #2 and #3 (integrity review SQ-3) — always cite Board questions with their section, not the bare number.
